{
  "name": "project",
  "scripts": {
    "dev": "wrangler dev",
    "build": "npm run build:static && npm run deploy:cloudflare",
    "build:static": "npm run clean && npm run compile",
    "deploy:cloudflare": "wrangler deploy"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "openai": "^4.52.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  }
}
